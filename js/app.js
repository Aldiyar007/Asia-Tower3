let textLoading = $('.text__loading');

if (!$('.slider').slick) {
    textLoading.text('У вас слабый интернет, слайдер загружается');
}

// if ($('.slider').slick) {
//     textLoading.text('');
// }